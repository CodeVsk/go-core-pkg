# go-core-pkg — Generic and Reusable Packages for Go

**go-core-pkg** brings together **domain-agnostic** and **reusable** functionality.
It centralizes common implementations that are typically duplicated across services — such as error handling,
telemetry, and environment variable loading.